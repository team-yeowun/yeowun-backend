폐기 사유: 대조군 Z1 꼬리 이탈 (loadtest-measure-protocol §3)

Z1(정적 파일, 대조군) r2 분포:
  min 0.65 / med 0.84 / p90 3.11 / p95 4.81 / max 6.51 ms  (n=10)

프로토콜 기준은 "Z1 1~2ms 유지"이고, STEP 4 측정은 p50·p95 모두 ≤2ms로 판정했다.
이 런은 p50은 정상(0.84ms)이나 10표본 중 1~2개가 3~6.5ms로 튀었다.
같은 스윕의 다른 두 런은 Z1 max가 1.4ms(100k)·1.3ms(1M)였다 — 이 런만 이탈이다.

→ 숫자를 보고하지 않고 폐기. 같은 파라미터로 500k를 재측정했다.
   재측정 결과: loadtest/results/ 의 이후 after-denorm-v500k 디렉토리.

폐기된 런의 산출물은 증거로 전량 보존한다(k6-r1~r5.json · explain.txt · app.log · meta.json · pools.json).
참고: 이 런의 위생 검사는 정상이었다(region_filled=500174 / is_free_rows=364344).
