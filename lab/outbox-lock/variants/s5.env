INGESTION_CLAIM_STRATEGY=REDIS_MARKER
INGESTION_DISPATCH_BATCH_SIZE=500
INGESTION_OUTBOX_READ=MASTER
# S5 Redis 행 마커 - 최종 운영값. 앱 2대 · 정규 런.
# 적재 5분 시점부터 60초간 마커 삭제를 주입한다(S5-2를 이 런 안에 접었다).
# 주입 구간은 deleted-keys.txt 의 타임스탬프로 갈라 읽는다.
LAB_INSTANCES=2
LAB_MODE=regular
LAB_INJECT_MARKER_LOSS=true
