INGESTION_CLAIM_STRATEGY=PESSIMISTIC
INGESTION_DISPATCH_BATCH_SIZE=500
INGESTION_OUTBOX_READ=REPLICA
# S4a 잠금 조회를 복제본으로. 앱 1대 · 단축 런.
# 기대: 복제본이 super_read_only 라 FOR UPDATE 를 거부하고 그 오류 원문이 앱 로그에 남는다.
#       거부되지 않더라도 복제본에서 잡은 잠금은 원본을 읽는 다른 인스턴스에게 보이지 않는다(논증 축 1).
LAB_INSTANCES=1
LAB_MODE=short
