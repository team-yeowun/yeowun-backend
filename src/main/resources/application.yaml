spring:
  application:
    name: backend
  # webflux 의존(WebClient) 추가했지만 웹 스택은 서블릿(MVC) 유지.
  main:
    web-application-type: servlet
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/mydatabase?connectionTimeZone=Asia/Seoul&forceConnectionTimeZoneToSession=true}
    username: ${SPRING_DATASOURCE_USERNAME:myuser}
    password: ${SPRING_DATASOURCE_PASSWORD:secret}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
        timezone:
          default_storage: NORMALIZE
  flyway:
    enabled: true
  data:
    redis:
      # AI '질문으로 작성' 임시저장(draft) 캐시. 로컬 bootRun은 spring-boot-docker-compose가 redis 서비스를 자동기동·자동연결.
      # Redis 장애여도 draft는 부가 기능이라 어댑터가 예외를 삼켜 degrade(질문/감상문 생성·저장은 정상).
      host: ${SPRING_DATA_REDIS_HOST:localhost}
      port: ${SPRING_DATA_REDIS_PORT:6379}
      password: ${SPRING_DATA_REDIS_PASSWORD:}
      timeout: 2s

management:
  endpoints:
    web:
      exposure:
        # prometheus: Alloy 에이전트가 스크랩해 Grafana Cloud로 remote_write.
        # ⚠️ 앱 포트가 공개(EC2 8080)라 actuator도 공개된다 — metrics와 동일 데이터라 노출 등급은 그대로지만,
        #    management 포트 분리/역프록시 차단은 별도 하드닝 과제(docs/observability-setup.md 참고).
        include: health,info,metrics,prometheus
  endpoint:
    health:
      probes:
        enabled: true
  metrics:
    tags:
      application: modi-backend   # 모든 지표에 앱 라벨(다중 인스턴스 구분용)
    distribution:
      # HTTP 요청 지연을 히스토그램(_bucket)으로 노출 → PromQL histogram_quantile로 p50/p95/p99 계산·집계 가능.
      # (기본은 summary라 _bucket이 없어 percentile 계산 불가) 버킷 상·하한을 두어 카디널리티를 억제한다.
      percentiles-histogram:
        "[http.server.requests]": true
      minimum-expected-value:
        "[http.server.requests]": 5ms
      maximum-expected-value:
        "[http.server.requests]": 3s

server:
  port: 8080

# CORS 허용 오리진 (공개 정보 — 시크릿 아님).
# 기본(프로파일 없음) = 로컬 개발용. 운영 배포는 prod 프로파일에서 Vercel 도메인으로 override.
app:
  # 로컬 시드 모드(로컬 default 프로파일=true). 켜지면 부팅 시 classpath 시드 SQL로만 전시를 초기화하고,
  #   외부 카탈로그 동기화(부팅·자정 스케줄러)는 skip한다 — application.yaml에 공공데이터 인증키 기본값이 박혀 있어도
  #   프론트 개발자의 도커 로컬 기동이 실 data.go.kr을 호출하지 않게 한다. 운영(prod 프로파일)은 명시적으로 false.
  local-seed:
    enabled: ${LOCAL_SEED_ENABLED:true}

  cors:
    allowed-origin-patterns:
      - http://localhost:3000   # 여운 프론트 로컬
      - http://localhost:3005   # 여운 프론트 로컬(대체 포트)
      - http://localhost:8080   # 백엔드 내장 HTML 테스트 페이지

  # OAuth 설정. client-id는 공개성 값이라 기본값을 둔다. client-secret은 시크릿 → 환경변수로만 주입.
  # redirect-uri는 콘솔에 등록된 값과 정확히 일치해야 한다(현재 테스트는 백엔드 내장 페이지 = :8080/login).
  oauth:
    redirect-uri: ${OAUTH_REDIRECT_URI:http://localhost:8080/login}
    # FE 주도 로그인에서 허용하는 redirectUri 화이트리스트 (로컬 프론트/테스트)
    allowed-redirect-uris:
      - http://localhost:3000/login
      - http://localhost:3005/login
      - http://localhost:8080/login
    kakao:
      # 운영 비즈앱 "여운"의 REST 키. client-id는 authorize URL에도 노출되는 공개값이라 코드 기본값을 둔다(네이버와 동일).
      # ⚠️ 프론트 oauth.js 의 KAKAO_CLIENT_ID 와 반드시 같아야 한다 — 프론트가 A 앱으로 받은 code를
      #    백엔드가 B 앱으로 교환하면 카카오가 거부한다(OAuthClientIdDefaultTest 가 이 쌍을 고정한다).
      # ⚠️ 구 테스트앱 "여운-TEST"(bba3e1d9…, 앱ID 1500036)는 카카오 콘솔에서 삭제됐다. 되돌리면
      #    토큰 교환이 KOE101(Not exist client_id)로 깨진다(2026-07-21 운영 장애).
      # client-secret은 시크릿 → env로만 주입. client-id를 바꾸면 secret도 그 앱 것으로 함께 갱신해야 한다.
      client-id: ${KAKAO_CLIENT_ID:bd5949f0127dd8ae068263f6ec1b5edc}
      client-secret: ${KAKAO_CLIENT_SECRET:}
      # 동의항목 요청: 이메일 + 연령대 + 출생연도.
      # ⚠️ 카카오 콘솔 [동의항목]에서 각 항목 ON 필수(테스트앱은 팀원만 검수 없이 동작).
      #    미승인 항목을 scope에 넣으면 authorize 시 KOE006 → 로그인 깨짐. 콘솔 준비 전이면 KAKAO_SCOPE 로 줄여 둘 것.
      scope: ${KAKAO_SCOPE:account_email,age_range,birthyear}
    naver:
      # client-id는 authorize URL·프론트에도 노출되는 공개값(카카오처럼) → 코드 기본값을 둔다.
      # ⚠️ 프론트 VITE_NAVER_CLIENT_ID 와 반드시 같아야 한다 — 프론트가 A 앱으로 받은 code를 백엔드가 B 앱으로
      #    교환하면 네이버가 거부하고 OAUTH_COMMUNICATION_FAILED 로만 보인다(2026-07-19 운영 장애: 네이버 앱을
      #    새로 발급하며 프론트만 갱신돼 구 기본값 ZRdsYHmN3ncMkx2F6353 이 남아 있었다).
      #    값을 바꾸면 OAuthClientIdDefaultTest 가 함께 깨지므로 프론트 동시 갱신 여부를 되묻게 된다.
      # client-secret은 시크릿 → env(GitHub Secret NAVER_CLIENT_SECRET)로만 주입. 로컬 실연동 시엔 로컬 env로 넣어야 토큰교환 성공.
      #    ⚠️ client-id를 새 앱으로 바꾸면 secret도 그 앱 것으로 함께 갱신해야 한다(쌍이 맞아야 통과).
      client-id: ${NAVER_CLIENT_ID:x5SSFHxy06Npeieb9JlA}
      client-secret: ${NAVER_CLIENT_SECRET:}

  # 공공데이터 전시 API(한눈에보는문화정보조회서비스, data.go.kr 15138937). (03_전시.md CATALOG 출처)
  # service-key: data.go.kr 일반 인증키. 공공데이터 오픈API 키라 저위험 + 재발급 가능해 yaml에 직접 둔다(팀 참고용).
  #   env(CULTURE_API_KEY)로 오버라이드 가능. 미설정·무효면 외부 동기화 스킵되고 데모 시드로 동작.
  public-data:
    culture:
      base-url: ${CULTURE_API_BASE_URL:https://apis.data.go.kr/B553457/cultureinfo}
      service-key: ${CULTURE_API_KEY:55aa921462823cce4b6aa1d36b0f5c318b0100a16af6fc56c89527e7353ad154}
      realm-code: D000        # 전시
      num-of-rows: 100
      max-pages: 5
      timeout-seconds: ${CULTURE_API_TIMEOUT_SECONDS:15}  # 무응답 stall 시 스케줄러 스레드·DB 커넥션 영구 점유 방지

  # 데모용 전시 시드. 켜면 부팅 시 표본 CATALOG/MOCK 전시를 적재한다(테이블이 비어 있을 때만).
  # 로컬 시연 전용 — 운영/테스트에서는 꺼 둔다(기본 false).
  exhibition:
    demo-seed:
      enabled: ${EXHIBITION_DEMO_SEED:false}
    # 전시 카탈로그 정기 동기화 스케줄. sync.cron 시각에 목록 동기화 + 상세 보강을 수행한다(기본 매일 자정).
    sync:
      cron: ${EXHIBITION_SYNC_CRON:0 0 0 * * *}
    # 전시 장르 분류(ADR-11). classifier로 주 분류기를 고른다(구현들 빈 공존, 무중단 교체).
    #   mock(기본, develop·로컬·CI): 제목 해시 결정적 분류 — AI 호출·비용 0, provider=MOCK 계보.
    #   gemini(운영 prod): 폴백 체인 = 1차 Gemini → 2차 Claude(resilience4j Retry+CircuitBreaker).
    #     전 공급자 실패 시 폴백값 없이 예외 → 아웃박스 RETRYABLE(무기한) → draft 승격 대기.
    #   claude.*: 2차 공급자 전용 버킷 — 감상문용 app.ai.*와 한도 분리(장르 백필이 감상문 한도를 안 태움).
    genre:
      classifier: ${GENRE_CLASSIFIER:mock}
      claude:
        api-key: ${GENRE_CLAUDE_API_KEY:}
        model: ${GENRE_CLAUDE_MODEL:claude-haiku-4-5-20251001}
    # 공공데이터(CATALOG) 보강 — 목록 동기화는 목록 필드만 적재하므로 장르(분류)·상세2(price 등)를 이어서 채운다.
    #   장르: 동기화(부팅 1회·매일 자정) "직후에만" 신규 적재분(미분류 행)을 배치로(배치당 AI 1콜) 분류한다.
    #        별도 주기 백필 없음 — 동기화가 신규만 추가하므로 미분류 행 = 방금 추가된 전시(멱등, 신규 없으면 no-op).
    #        부팅 분류는 데몬 스레드로 수행해 readiness/헬스체크를 막지 않는다.
    #   상세(가격 등): 별도 백필 잡 없이 syncCatalog가 목록+상세를 한 패스로 받아 적재 시점에 함께 채운다(무료 섹션이 실제로 뜨게 하는 근본 값).
    enrich:
      genre-batch-size: ${ENRICH_GENRE_BATCH_SIZE:20}  # 60s 타임아웃 안에서 검증된 크기(키우면 ReadTimeout→랜덤 폴백 위험)
      genre-max-batches-per-run: ${ENRICH_GENRE_MAX_BATCHES:3}  # 회당 소량 → 무료 RPM 안 넘김(대량 백로그는 다음 동기화가 이어서 소진)
    # 전시 영업시간(운영시간) — 구글 Places(New)로 place_addr를 조회해 exhibitions.operating_hours를 채운다.
    #   provider: 유료호출 게이팅. 기본 mock(로컬·CI·develop 0콜, 고정 샘플). 운영(main)만 google + GOOGLE_MAPS_API_KEY 주입.
    #     google이라도 api-key가 비면 mock로 폴백(안전 기본값). 원본은 google_place_hours(매 실행 초기화 스테이징).
    #   대상 선별: 주소 있고 미조회거나 refresh-after-days보다 오래된 전시만 → 매일 돌아도 스테디 상태 호출 ≈ 0(신규/만료만).
    place-hours:
      provider: ${PLACE_HOURS_PROVIDER:mock}
      base-url: ${GOOGLE_MAPS_BASE_URL:https://places.googleapis.com}
      api-key: ${GOOGLE_MAPS_API_KEY:}
      language-code: ko
      region-code: KR
      timeout-seconds: ${GOOGLE_MAPS_TIMEOUT_SECONDS:10}      # 무응답 stall 시 스레드·커넥션 장기 점유 방지
      refresh-after-days: ${PLACE_HOURS_REFRESH_AFTER_DAYS:30} # 이보다 오래 전에 조회된 장소만 재호출
      max-venues-per-run: ${PLACE_HOURS_MAX_VENUES_PER_RUN:100} # 실행당 장소 호출 상한(일 과금 캡)
    # 전시 아웃박스(exhibition_outbox) — at-least-once 재시도 파이프라인(상세·장르·영업시간 재검증의 단일 상태머신, ADR-10).
    #   메시지는 상태 변경과 같은 트랜잭션에서 적재되고(원자성), 상태가 DB에 남아 재시작 후에도 이어서 처리된다.
    #   릴레이 = 스케줄 폴링(durable 엔진) + 커밋 직후 이벤트 드레인(글루 — 유실돼도 폴링이 줍는다).
    #   재시도 분류: timeout/5xx/429=RETRYABLE(지수 백오프), 4xx/파싱실패=PERMANENT. 최대 시도 초과 시 승격(무한 재시도 방지).
    outbox:
      max-attempts: ${OUTBOX_MAX_ATTEMPTS:5}                 # 재시도 가능한 실패를 몇 번까지(초과 시 FAILED_PERMANENT)
      base-backoff-seconds: ${OUTBOX_BASE_BACKOFF_SECONDS:60} # 지수 백오프 기준 간격(첫 재시도까지)
      max-backoff-seconds: ${OUTBOX_MAX_BACKOFF_SECONDS:3600} # 백오프 상한(간격이 무한정 벌어지지 않게)
      batch-size: ${OUTBOX_BATCH_SIZE:50}                    # 한 드레인에서 집는 메시지 수 상한
      poll-interval-ms: ${OUTBOX_POLL_INTERVAL_MS:60000}     # 릴레이 폴링 주기(ms) — durable 엔진의 심박
      hours-refresh-min-interval-days: ${OUTBOX_HOURS_REFRESH_MIN_INTERVAL_DAYS:30} # 이벤트 재검증 최소 간격(설계 §4-1 가드)

  # 리마인드(오늘의 여운) — 기록 작성 후 소환 대상이 되기까지의 경과 시간. 스케줄 발송이 아니라 조회 시점 판정(pull)이라
  #   이 값만 바꾸면 즉시 반영된다. 정식 기본 7d(운영 베타 한정 단축은 prod 프로파일 참고).
  remind:
    eligible-after: ${REMIND_ELIGIBLE_AFTER:7d}
    # 감정 변화 AI 요약 백필 — 요약은 저장 응답을 막지 않으려 인메모리 스레드풀에서 만든다.
    #   요약 도중 재시작·배포되면 그 작업이 유실돼 PENDING이 영구히 남으므로, 이 잡이 주기적으로 복구한다.
    #   유료 AI 호출이라 batch-size로 1회 실행량을 제한하고, grace 이전 건만 대상으로 해 진행 중 작업과 겹치지 않게 한다.
    #   give-up-after를 넘긴 건은 호출 없이 FAILED 확정 → 클라이언트 무한 폴링 방지.
    summary-backfill:
      enabled: ${REMIND_SUMMARY_BACKFILL_ENABLED:true}
      interval: ${REMIND_SUMMARY_BACKFILL_INTERVAL:PT10M}
      initial-delay: ${REMIND_SUMMARY_BACKFILL_INITIAL_DELAY:PT2M}
      batch-size: ${REMIND_SUMMARY_BACKFILL_BATCH_SIZE:20}
      grace: ${REMIND_SUMMARY_BACKFILL_GRACE:PT2M}
      give-up-after: ${REMIND_SUMMARY_BACKFILL_GIVE_UP_AFTER:P1D}

  # 관리자 콘솔 로그인 자격(이메일 화이트리스트 + 공용 비밀번호). 로그인하면 admin_session 쿠키로 /api-admin/** 접근.
  #   ⚠️ 레포가 PUBLIC이라 값은 절대 여기 넣지 말 것 → env/Secret으로만 주입. 미설정이면 아무도 로그인 못 함(안전 기본값).
  #   ADMIN_EMAILS=a@x.com,b@y.com  ·  ADMIN_PASSWORD=<공용비번>
  admin:
    emails: ${ADMIN_EMAILS:}
    password: ${ADMIN_PASSWORD:}

  # AI 감상문 생성(질문/compose). provider 교체 가능 — claude(Anthropic) | gemini(Google).
  #   AI_PROVIDER로 어댑터를 전환한다(코드 변경 불필요). 예: AI_PROVIDER=gemini.
  #   api-key 미설정이면 AI 엔드포인트는 503(AI_DISABLED)로 응답하고 나머지 기능은 정상 동작한다.
  ai:
    provider: ${AI_PROVIDER:claude}
    # 미지정 시 provider별 기본 모델(claude-sonnet-5 / gemini-2.5-flash-lite). gemini 기본을 flash-lite로 —
    #   무료 일일 한도가 flash보다 훨씬 크고 감상문 질문/다듬기엔 품질 충분. 장르 분류(app.ai.gemini, 기본 flash)와
    #   모델을 다르게 유지해 무료 한도 버킷을 분리한다(같은 모델로 맞추면 한도를 공유하게 되니 주의).
    model: ${AI_MODEL:}
    # 키는 provider와 짝을 이뤄 명시한다(교차 오염 방지): 공통 AI_API_KEY > Claude ANTHROPIC_API_KEY.
    #   gemini 사용 시엔 AI_PROVIDER=gemini + AI_API_KEY=<Gemini 키>(예: AI_API_KEY=${GEMINI_API_KEY}).
    #   ※ GEMINI_API_KEY를 여기 자동 폴백에 넣지 않는다 — 배포 env에 GEMINI_API_KEY만 있고 provider=claude면
    #     Claude 어댑터가 Gemini 키를 오용(AI_GENERATION_FAILED)하기 때문. 장르 분류는 별도로 app.ai.gemini에서 GEMINI_API_KEY 사용.
    api-key: ${AI_API_KEY:${ANTHROPIC_API_KEY:}}
    max-tokens: ${AI_MAX_TOKENS:768}               # ≤300자 감상문/요약·질문 3개에 충분하면서 폭주 생성 바운드(M-4). 상향은 AI_MAX_TOKENS.
    timeout-seconds: ${AI_TIMEOUT_SECONDS:30}     # 외부 LLM 호출 타임아웃(워커 스레드 장기 점유 방지)
    rate-limit-seconds: ${AI_RATE_LIMIT_SECONDS:3} # 사용자당 AI 호출 최소 간격(반복 클릭 폭주 방지)
    max-retries: ${AI_MAX_RETRIES:2}               # 429(무료 RPM 순간 초과) 시 재시도 횟수(Retry-After 존중, 소진 시 AI_RATE_LIMITED)
    max-retry-delay-seconds: ${AI_MAX_RETRY_DELAY_SECONDS:4}  # 재시도 백오프 상한(대화형이라 과도한 대기 방지)
    draft:
      ttl: ${AI_DRAFT_TTL:1h}                      # AI 임시저장(draft) 캐시 TTL(뒤로가기 복원용, 방치 draft는 자동 만료)

    # 전시 장르 분류용 Gemini(무료 한도). api-key 미설정이면 AI 분류기가 random으로 폴백(나머지 기능 정상).
    #   model 기본을 flash로 — 감상문 AI(app.ai, 기본 flash-lite)와 "서로 다른 모델"을 써서 무료 한도 버킷을 분리한다
    #   (Gemini 무료 한도는 모델별 — 장르 분류가 감상문 AI 한도를 갉아먹지 않게). 장르는 동기화 직후 신규분만
    #   분류하므로 호출량이 적어 flash의 상대적으로 작은 일일 한도로도 충분하다.
    gemini:
      base-url: ${GEMINI_BASE_URL:https://generativelanguage.googleapis.com}
      api-key: ${GEMINI_API_KEY:}
      model: ${GEMINI_MODEL:gemini-2.5-flash}
      timeout-seconds: ${GEMINI_TIMEOUT_SECONDS:60}   # 배치(초기화)는 여러 전시를 한 응답으로 받아 오래 걸림 → 여유 있게(단일 호출·초과 시 랜덤 폴백)
      max-retries: ${GEMINI_MAX_RETRIES:1}                    # 429 시 재시도 횟수(Retry-After 존중)
      max-retry-delay-seconds: ${GEMINI_MAX_RETRY_DELAY_SECONDS:2}  # 재시도 백오프 상한(부팅 지연 방지)

  # 자체 JWT 설정. secret은 시크릿 → 환경변수 주입 권장(기본값은 로컬 테스트 전용).
  jwt:
    secret: ${JWT_SECRET:local-dev-only-jwt-secret-please-change-min-32-bytes}
    access-ttl-seconds: 900        # access token 15분
    refresh-ttl-seconds: 1209600   # refresh token 14일

  # refresh 쿠키 속성. 로컬(http)은 secure off.
  auth:
    cookie:
      secure: false
      same-site: Lax

---
# 운영(EC2) 프로파일: 배포 시 SPRING_PROFILES_ACTIVE=prod 로 활성화된다.
spring:
  config:
    activate:
      on-profile: prod
app:
  # 운영은 실 카탈로그 동기화를 쓴다 — 로컬 시드 모드 명시적 off(SPRING_PROFILES_ACTIVE=prod 주입과 이중 안전).
  local-seed:
    enabled: false
  exhibition:
    # 운영에서만 AI 체인(1차 gemini → 2차 claude) 장르 분류를 쓴다. develop·로컬(기본 프로파일)은 mock(무비용·결정적).
    genre:
      classifier: ${GENRE_CLASSIFIER:gemini}
  # ⚠️ 베타 한정: 리마인드 소환 대기를 3초로 단축(2~3일 베타에서 즉시 검증용, 리마인드는 조회 시점 판정이라 값만 줄이면 됨).
  #   베타 종료 후 REMIND_ELIGIBLE_AFTER=7d 주입(또는 이 줄 제거로 기본 7d 복귀).
  remind:
    eligible-after: ${REMIND_ELIGIBLE_AFTER:3s}
  cors:
    allowed-origin-patterns:
      - https://yeowun.vercel.app      # 여운 Vercel 운영 도메인
      - https://yeowun-demo.vercel.app # 여운 베타테스트 데모 프론트(별도 레포 yeowun-demo)
  oauth:
    # 운영 redirect_uri = 여운 프론트 운영 콜백 (콘솔 등록값과 일치). 필요 시 env로 override.
    redirect-uri: ${OAUTH_REDIRECT_URI:https://yeowun.vercel.app/login}
    allowed-redirect-uris:
      - https://yeowun.vercel.app/login
      - https://yeowun-demo.vercel.app/login # 데모 프론트 카카오 콜백 (콘솔에도 동일 등록 필요)
  # 운영: HTTPS(yeowun.vercel.app, Vercel 프록시로 same-origin)라 Secure 켜고 SameSite=Lax.
  auth:
    cookie:
      secure: true
      same-site: Lax
